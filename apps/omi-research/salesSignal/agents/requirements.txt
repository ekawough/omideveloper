# Pin majors to avoid CrewAI/Pydantic drift that has bitten people before.
crewai==1.14.1
crewai-tools==0.70.0
fastapi==0.115.4
uvicorn[standard]==0.32.0
pydantic==2.9.2
requests==2.32.3
python-dotenv==1.0.1

# Supabase client (async-capable).
supabase==2.9.1

# OpenAI SDK — we route through CrewAI's `openai/` provider prefix.
openai==1.54.3

# Acoustic emotion — primary: SenseVoice via FunASR.
# Install torch separately; on AMD ROCm use the rocm index URL.
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/rocm6.0
funasr==1.1.12
torch>=2.3
torchaudio>=2.3
numpy>=1.26

# Backup emotion: SpeechBrain (verified ROCm support).
speechbrain==1.0.2

# Optional prosodic features for coaching dashboard (CPU only).
opensmile==2.5.0
