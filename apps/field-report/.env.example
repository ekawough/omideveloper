# Field Report — Environment Variables
# Copy to .env and fill in your values
# On Railway: set these in the dashboard under Variables

# Required
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Optional — port defaults to 3000
PORT=3000
